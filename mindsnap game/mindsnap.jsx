import React, { useCallback, useEffect, useMemo, useState } from "react";

const T = { ink: "#171329", violet: "#7567f8", cyan: "#56d8d0", pink: "#ff6fae", yellow: "#ffd166", bg: "#f6f7fc", card: "#ffffff", muted: "#77748a", line: "#e7e5f0", good: "#3bc58a", bad: "#ff6b6b" };
const MODES = {
  casual: { name: "Casual", tag: "WARM UP", icon: "☄", grid: 3, targets: 3, rounds: 3, seconds: 4, color: T.cyan, copy: "A calm start to sharpen your focus." },
  focused: { name: "Focused", tag: "MOST PLAYED", icon: "✦", grid: 4, targets: 5, rounds: 4, seconds: 3, color: T.violet, copy: "Build a pattern. Keep it in your head." },
  expert: { name: "Expert", tag: "NO MERCY", icon: "⚡", grid: 5, targets: 7, rounds: 5, seconds: 2, color: T.pink, copy: "For players who want a serious brain workout." },
};
const DEFAULT_STATS = { games: 0, best: 0, hits: 0, attempts: 0, streak: 0, lastPlayed: null };
const loadStats = () => { try { return { ...DEFAULT_STATS, ...JSON.parse(localStorage.getItem("mindsnap-stats") || "{}") }; } catch { return DEFAULT_STATS; } };
const persistStats = (value) => { try { localStorage.setItem("mindsnap-stats", JSON.stringify(value)); } catch {} };
const percent = (a, b) => b ? Math.round((a / b) * 100) : 0;
const shuffle = (items) => [...items].sort(() => Math.random() - 0.5);

function SpeechButton({ text }) { const [available] = useState(() => typeof window !== "undefined" && "speechSynthesis" in window); const speak = () => { if (!available) return; window.speechSynthesis.cancel(); window.speechSynthesis.speak(new SpeechSynthesisUtterance(text)); }; return available ? <button className="sound" onClick={speak}>◖ Hear how to play</button> : null; }

function Header({ game, leave }) { return <header className="header"><button className="logo" onClick={leave}><span>✦</span> MINDSNAP</button><div className="header-right"><span className="status"><i /> LOCAL SAVE</span>{game && <button className="exit" onClick={leave}>Quit game</button>}</div></header>; }

function Stat({ value, label, color = T.violet }) { return <div className="stat"><b style={{ color }}>{value}</b><span>{label}</span></div>; }

function Home({ stats, start }) { const accuracy = percent(stats.hits, stats.attempts); return <main className="home page"><section className="hero"><div className="hero-copy"><div className="eyebrow"><span>VISUAL MEMORY ARCADE</span><i>v1.0</i></div><h1>See it.<br /><strong>Snap it.</strong></h1><p>Remember the glowing pattern, then rebuild it before your focus slips.</p><div className="hero-actions"><button className="play-button" onClick={() => start("focused")}>PLAY NOW <b>↗</b></button><SpeechButton text="Watch the glowing tiles. When they disappear, tap every tile that was glowing. Clear three rounds to finish your game." /></div></div><div className="hero-board"><div className="board-orbit o1" /><div className="board-orbit o2" /><div className="mini-grid">{Array.from({ length: 9 }, (_, i) => <span key={i} className={[1, 3, 7].includes(i) ? "lit" : ""}>{[1, 3, 7].includes(i) ? "✦" : ""}</span>)}</div><small>MEMORY<br />LOADED</small></div></section><section className="stats"><Stat value={stats.games} label="Games played" color={T.cyan} /><Stat value={`${accuracy}%`} label="Recall accuracy" color={T.violet} /><Stat value={stats.best} label="Best score" color={T.yellow} /><Stat value={`${stats.streak} day${stats.streak === 1 ? "" : "s"}`} label="Play streak" color={T.pink} /></section><section className="mode-heading"><div><div className="eyebrow"><span>CHOOSE YOUR RUN</span></div><h2>Pick a difficulty</h2></div><p>Every tap counts. Every game saves.</p></section><section className="modes">{Object.entries(MODES).map(([id, mode]) => <button className="mode" key={id} style={{ "--mode": mode.color }} onClick={() => start(id)}><div className="mode-top"><span className="mode-icon">{mode.icon}</span><em>{mode.tag}</em><b>↗</b></div><h3>{mode.name}</h3><p>{mode.copy}</p><footer><span>{mode.grid}×{mode.grid} board</span><span>{mode.rounds} rounds</span><span>{mode.seconds}s view</span></footer></button>)}</section><p className="save-note"><span>⌁</span> Your progress is stored on this device only.</p></main>; }

function PatternBoard({ mode, targets, revealed, picked, onPick, flash }) { const size = mode.grid; return <div className={`pattern-board grid-${size} ${flash ? "flash" : ""}`}>{Array.from({ length: size * size }, (_, i) => { const active = revealed && targets.includes(i); const selected = picked.includes(i); return <button key={i} onClick={() => onPick(i)} className={`tile ${active ? "active" : ""} ${selected ? "selected" : ""}`} disabled={revealed || selected}><span>{active || selected ? "✦" : ""}</span></button>; })}</div>; }

function Game({ mode, round, targets, picked, revealed, seconds, score, misses, onPick, quit }) { const config = MODES[mode]; return <main className="game page"><div className="game-top"><button className="back" onClick={quit}>← Menu</button><div className="run-label"><span style={{ background: config.color }}>{config.name}</span><b>ROUND {round} / {config.rounds}</b></div><div className="game-score"><small>SCORE</small><b>{score}</b></div></div><div className="game-progress"><i style={{ width: `${(round - 1) / config.rounds * 100}%`, background: config.color }} /></div><section className="game-intro"><div><p className="eyebrow">{revealed ? "MEMORIZE THE PATTERN" : "REBUILD THE PATTERN"}</p><h1>{revealed ? "Lock it in." : "Your turn."}</h1><p>{revealed ? `Remember all ${config.targets} glowing tiles.` : "Tap every tile that was glowing. Order does not matter."}</p></div><div className="game-metrics"><span><b style={{ color: config.color }}>{revealed ? seconds : picked.length}</b><small>{revealed ? "seconds" : `/${config.targets} found`}</small></span><span><b style={{ color: T.bad }}>{misses}</b><small>misses</small></span></div></section><PatternBoard mode={config} targets={targets} revealed={revealed} picked={picked} onPick={onPick} flash={!revealed && misses > 0} /><p className="board-tip">{revealed ? "The pattern will disappear when the timer ends." : "Find the glowing tiles to complete the round."}</p></main>; }

function Results({ result, stats, mode, replay, home }) { const config = MODES[mode]; const accuracy = percent(result.hits, result.hits + result.misses); return <main className="results page"><div className="result-burst">{accuracy >= 80 ? "✦" : "◌"}</div><p className="eyebrow">RUN COMPLETE · {config.name.toUpperCase()}</p><h1>{accuracy >= 80 ? "Pattern locked." : accuracy >= 50 ? "You held your own." : "The brain is warming up."}</h1><p className="result-copy">A real result from your taps, misses and completed rounds.</p><section className="final-score"><small>FINAL SCORE</small><b>{result.score}</b><span>points</span></section><section className="result-stats"><Stat value={`${accuracy}%`} label="Recall accuracy" color={T.cyan} /><Stat value={`${result.hits}/${result.hits + result.misses}`} label="Tiles recalled" color={T.violet} /><Stat value={result.misses} label="Misses" color={T.pink} /><Stat value={`#${stats.games}`} label="Game number" color={T.yellow} /></section><div className="result-actions"><button className="play-button" onClick={replay}>RUN IT BACK <b>↗</b></button><button className="secondary" onClick={home}>Back to menu</button></div></main>; }

export default function Mindsnap() { const [screen, setScreen] = useState("home"); const [mode, setMode] = useState("focused"); const [round, setRound] = useState(1); const [targets, setTargets] = useState([]); const [picked, setPicked] = useState([]); const [revealed, setRevealed] = useState(false); const [seconds, setSeconds] = useState(0); const [score, setScore] = useState(0); const [misses, setMisses] = useState(0); const [roundHits, setRoundHits] = useState(0); const [result, setResult] = useState(null); const [stats, setStats] = useState(loadStats); const config = MODES[mode];
  const createPattern = useCallback((id, number) => { const chosen = MODES[id]; const cells = shuffle(Array.from({ length: chosen.grid * chosen.grid }, (_, i) => i)).slice(0, chosen.targets); setMode(id); setRound(number); setTargets(cells); setPicked([]); setRevealed(true); setSeconds(chosen.seconds); setScreen("game"); }, []);
  useEffect(() => { if (!revealed || screen !== "game") return; if (seconds <= 0) { setRevealed(false); return; } const timer = setTimeout(() => setSeconds(s => s - 1), 1000); return () => clearTimeout(timer); }, [revealed, screen, seconds]);
  useEffect(() => { if (screen !== "game" || revealed || picked.length !== config.targets) return; const delay = setTimeout(() => { const roundScore = Math.max(0, config.targets * 100 - misses * 30); const newScore = score + roundScore; const newHits = roundHits + config.targets; if (round < config.rounds) { setScore(newScore); setRoundHits(newHits); createPattern(mode, round + 1); } else { const final = { score: newScore, hits: newHits, misses }; const today = new Date().toISOString().slice(0, 10); const yesterday = stats.lastPlayed && Date.parse(today) - Date.parse(stats.lastPlayed) === 86400000; const fresh = { ...stats, games: stats.games + 1, best: Math.max(stats.best, newScore), hits: stats.hits + newHits, attempts: stats.attempts + newHits + misses, streak: stats.lastPlayed === today ? stats.streak : yesterday ? stats.streak + 1 : 1, lastPlayed: today }; setResult(final); setStats(fresh); persistStats(fresh); setScreen("results"); } }, 550); return () => clearTimeout(delay); }, [picked, revealed, screen]);
  const start = id => { setScore(0); setMisses(0); setRoundHits(0); setResult(null); createPattern(id, 1); };
  const pick = cell => { if (targets.includes(cell)) { setPicked(p => [...p, cell]); setScore(s => s + 10); } else { setMisses(m => m + 1); } };
  const home = () => { setScreen("home"); setResult(null); };
  return <div className="app"><style>{CSS}</style><Header game={screen === "game"} leave={home} />{screen === "home" && <Home stats={stats} start={start} />}{screen === "game" && <Game mode={mode} round={round} targets={targets} picked={picked} revealed={revealed} seconds={seconds} score={score} misses={misses} onPick={pick} quit={home} />}{screen === "results" && result && <Results result={result} stats={stats} mode={mode} replay={() => start(mode)} home={home} />}</div>; }

const CSS = `@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap');*{box-sizing:border-box}body{margin:0;background:${T.bg};color:${T.ink};font-family:'DM Sans',system-ui,sans-serif}.app{min-height:100vh;background:radial-gradient(circle at 84% 0%,#e9e7ff 0,transparent 30%),${T.bg}}button{font:inherit;color:inherit}header{height:78px;max-width:1200px;margin:auto;padding:0 32px;display:flex;align-items:center;justify-content:space-between}.logo{border:0;background:none;font:700 17px 'Space Grotesk';letter-spacing:1.4px;cursor:pointer}.logo span{display:inline-grid;place-items:center;width:31px;height:31px;border-radius:10px;background:${T.ink};color:${T.cyan};font-size:20px;margin-right:9px;vertical-align:middle}.header-right{display:flex;align-items:center;gap:18px;color:${T.muted};font-size:10px;letter-spacing:1px}.status i{display:inline-block;width:7px;height:7px;border-radius:50%;background:${T.good};margin-right:6px}.exit,.back{border:0;background:none;color:${T.muted};cursor:pointer;font-size:12px}.page{max-width:1100px;margin:auto;padding:46px 32px 76px}.hero{display:flex;align-items:center;justify-content:space-between;min-height:360px}.eyebrow{display:flex;align-items:center;gap:10px;font-size:10px;letter-spacing:1.9px;font-weight:700;color:${T.muted};margin:0 0 15px}.eyebrow i{font-style:normal;color:${T.violet};letter-spacing:0}.hero h1{font:700 72px/.92 'Space Grotesk';letter-spacing:-4px;margin:0}.hero h1 strong{color:${T.violet}}.hero-copy>p:not(.eyebrow){font-size:17px;color:${T.muted};line-height:1.55;max-width:405px;margin:24px 0}.hero-actions,.result-actions{display:flex;align-items:center;gap:15px}.play-button{border:0;background:${T.ink};color:#fff;border-radius:11px;padding:14px 17px;font-weight:700;font-size:12px;letter-spacing:1px;cursor:pointer;box-shadow:0 9px 20px #17132922}.play-button b{color:${T.cyan};font-size:17px;margin-left:17px}.sound{border:0;background:none;color:${T.muted};font-size:12px;cursor:pointer}.hero-board{position:relative;width:350px;height:300px;display:grid;place-items:center;margin-right:50px}.hero-board:before{content:"";position:absolute;width:220px;height:220px;background:#e4e2ff;border-radius:45%;transform:rotate(12deg)}.mini-grid{z-index:1;width:204px;height:204px;display:grid;grid-template-columns:repeat(3,1fr);gap:9px;padding:9px;border-radius:19px;background:#fff;box-shadow:0 24px 48px #6359bd24;transform:rotate(-5deg)}.mini-grid span{display:grid;place-items:center;background:#f1f0fb;border-radius:11px;color:transparent;font-size:22px}.mini-grid span.lit{background:${T.violet};color:#fff;box-shadow:0 0 22px #7567f899}.hero-board small{z-index:2;position:absolute;bottom:13px;right:5px;text-align:center;font:700 9px/1.2 'Space Grotesk';letter-spacing:1px;color:${T.violet};transform:rotate(8deg)}.board-orbit{position:absolute;border:1px solid #bdb7fd;border-radius:50%;width:310px;height:125px;transform:rotate(26deg)}.board-orbit.o2{width:280px;height:170px;transform:rotate(-39deg);border-color:#c7eee9}.stats,.result-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:${T.line};border:1px solid ${T.line};border-radius:15px;overflow:hidden;margin-bottom:75px}.stat{background:${T.card};padding:20px 23px;display:flex;flex-direction:column;gap:5px}.stat b{font:700 28px 'Space Grotesk'}.stat span{font-size:11px;color:${T.muted}}.mode-heading{display:flex;justify-content:space-between;align-items:end;margin-bottom:20px}.mode-heading h2{font:600 29px 'Space Grotesk';margin:0;letter-spacing:-1px}.mode-heading p{color:${T.muted};font-size:12px}.modes{display:grid;grid-template-columns:repeat(3,1fr);gap:15px}.mode{text-align:left;border:1px solid ${T.line};border-top:4px solid var(--mode);background:${T.card};border-radius:14px;padding:19px;cursor:pointer;transition:.2s}.mode:hover{transform:translateY(-5px);box-shadow:0 15px 30px #17132912}.mode-top{display:flex;align-items:center;gap:10px}.mode-icon{font-size:25px}.mode-top em{font-style:normal;color:var(--mode);font-size:9px;font-weight:700;letter-spacing:1.5px}.mode-top b{margin-left:auto;color:var(--mode);font-size:20px}.mode h3{font:600 26px 'Space Grotesk';margin:22px 0 7px}.mode>p{font-size:13px;line-height:1.45;color:${T.muted};min-height:38px}.mode footer{display:flex;justify-content:space-between;border-top:1px solid ${T.line};padding-top:13px;color:${T.muted};font-size:10px}.save-note{text-align:center;margin:44px 0 0;color:${T.muted};font-size:11px}.save-note span{color:${T.violet};font-size:18px;margin-right:6px}.game{max-width:850px;padding-top:26px}.game-top{display:flex;align-items:center;justify-content:space-between}.run-label{display:flex;align-items:center;gap:10px}.run-label span{color:#fff;border-radius:20px;padding:7px 12px;font-size:10px;font-weight:700}.run-label b{color:${T.muted};font-size:10px;letter-spacing:1px}.game-score{text-align:right}.game-score small{display:block;color:${T.muted};font-size:9px;letter-spacing:1px}.game-score b{font:700 24px 'Space Grotesk';color:${T.violet}}.game-progress{height:4px;background:${T.line};border-radius:5px;margin-top:24px}.game-progress i{display:block;height:100%;border-radius:5px}.game-intro{display:flex;justify-content:space-between;align-items:end;margin:50px 0 27px}.game-intro h1{font:700 42px 'Space Grotesk';letter-spacing:-1.7px;margin:0 0 7px}.game-intro p:not(.eyebrow){margin:0;color:${T.muted};font-size:14px}.game-metrics{display:flex;gap:26px}.game-metrics span{display:flex;flex-direction:column;text-align:right}.game-metrics b{font:700 28px 'Space Grotesk'}.game-metrics small{color:${T.muted};font-size:10px}.pattern-board{width:min(510px,80vw);aspect-ratio:1;margin:auto;display:grid;gap:10px;padding:10px;background:#ebeaf3;border-radius:19px;box-shadow:0 22px 44px #17132915}.grid-3{grid-template-columns:repeat(3,1fr)}.grid-4{grid-template-columns:repeat(4,1fr)}.grid-5{grid-template-columns:repeat(5,1fr);gap:7px;padding:8px}.tile{border:0;border-radius:12px;background:#fff;cursor:pointer;transition:.18s;box-shadow:inset 0 -3px 0 #dad8e7}.tile:hover:not(:disabled){transform:scale(.95);background:#f7f6ff}.tile.active,.tile.selected{background:var(--tile-color,${T.violet});color:#fff;box-shadow:0 0 25px #7567f899}.tile.active span,.tile.selected span{font-size:28px}.tile.selected{background:${T.good};box-shadow:0 0 18px #3bc58a88}.flash .tile{animation:shake .25s}.board-tip{text-align:center;color:${T.muted};font-size:11px;margin-top:17px}.results{text-align:center;padding-top:65px}.result-burst{display:grid;place-items:center;width:70px;height:70px;margin:auto auto 22px;border-radius:22px;background:#e7e5ff;color:${T.violet};font-size:40px}.results h1{font:700 43px 'Space Grotesk';letter-spacing:-1.5px;margin:0}.result-copy{color:${T.muted};font-size:14px;margin:14px 0 28px}.final-score{max-width:330px;margin:auto;background:${T.ink};color:#fff;border-radius:16px;padding:19px}.final-score small{font-size:9px;letter-spacing:1.6px;color:#aaa7c4}.final-score b{display:block;color:${T.cyan};font:700 60px 'Space Grotesk';line-height:1.1}.final-score span{font-size:11px;color:#aaa7c4}.result-stats{margin:20px 0 27px;text-align:left}.result-stats .stat{padding:17px}.secondary{border:1px solid ${T.line};background:${T.card};border-radius:11px;padding:14px 17px;font-weight:700;font-size:12px;cursor:pointer}@keyframes shake{0%,100%{transform:translateX(0)}25%{transform:translateX(-4px)}75%{transform:translateX(4px)}}@media(max-width:700px){header{height:66px;padding:0 18px}.header-right{font-size:0}.status i{font-size:initial}.page{padding:28px 18px 55px}.hero{display:block;min-height:0}.hero h1{font-size:59px}.hero-board{width:260px;height:240px;margin:23px auto 0}.stats{grid-template-columns:1fr 1fr;margin:45px 0}.stat{padding:16px}.mode-heading{display:block}.mode-heading p{margin-top:8px}.modes{grid-template-columns:1fr}.mode{padding:17px}.game-intro{align-items:start;display:block;margin-top:38px}.game-intro h1{font-size:36px}.game-metrics{margin-top:20px;justify-content:flex-start}.game-metrics span{text-align:left}.pattern-board{width:min(88vw,450px)}.run-label{flex-direction:column;align-items:flex-start;gap:5px}.results{padding-top:38px}.results h1{font-size:34px}.result-stats{grid-template-columns:1fr 1fr}.result-actions{flex-direction:column}.result-actions>*{width:100%}}`;
